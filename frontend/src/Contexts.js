import { createContext } from "react";
export const Change_Theme_context = createContext(),
  delivery_orders_context = createContext(),
  delivery_history_context = createContext(),
  AuthContext = createContext();
